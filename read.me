These scripts are to use the NRF24 transmitter with a Raspberry Pi and Python
To start these on reboot 

1. put @reboot /etc/init.d/script.sh in your ROOT crontab (so sudo crontab)
2. Make the script executable of course, or call your python script on reboot also possible
